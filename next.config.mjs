/** @type {import('next').NextConfig} */
const nextConfig = {};

// const nextConfig = {
//   webpack(config, { isServer }) {
//     // Set the target to 'web'
//     config.target = 'web';

//     // Example: Customize other Webpack configurations here
//     if (!isServer) {
//       // Client-side specific configurations
//     }

//     return config;
//   },
// };

export default nextConfig;
